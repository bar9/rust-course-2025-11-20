[package]
name = "temp_protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
postcard = { version = "1.0", features = ["alloc"] }
temp_core = { path = "../temp_core" }
temp_store = { path = "../temp_store" }
